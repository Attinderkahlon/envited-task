type Props = {
}

export default function Homepage() {
    return (
        <div className="flex  bg-envited-secondary-100 h-screen w-screen">
            <div><h1>Imagine if <span>Snapchat</span> had events</h1></div>
        </div>
    );
}